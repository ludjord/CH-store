{
  "name": "chstore-mern",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && mkdir -p dist && cp -r frontend/dist/* dist/"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.2",
    "jsonwebtoken": "^9.0.3",
    "midtrans-client": "^1.4.3",
    "mongoose": "^9.4.1",
    "nodemailer": "^8.0.5"
  }
}
